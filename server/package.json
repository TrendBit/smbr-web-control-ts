{
  "name": "smbr-web-control-ts-server",
  "version": "1.0.0",
  "description": "",
  "license": "GPL-3.0",
  "author": "David Radek",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "npm run build && node dist/src/server.js --no-frontend -d -v",
    "build": "rimraf dist && tsc",
    "start": "npm run build && node dist/src/server.js",
    "start:debug": "npm run build && node dist/src/server.js -d"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "chalk": "^5.6.2",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.5",
    "rimraf": "^6.1.3",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
